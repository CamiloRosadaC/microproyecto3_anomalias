# Copia este archivo a ".env" y completa los valores reales.
# La UI (app.py) leerá estas variables.

# URL de scoring del Online Endpoint (scoring_uri)
AML_URL=https://<tu-endpoint>.<region>.inference.ml.azure.com/score

# Clave primaria del Online Endpoint (Authorization: Bearer <clave>)
AML_KEY=<pega_aqui_tu_primaryKey>
